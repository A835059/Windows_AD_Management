---
- name : User Details
  win_domain_user:
    name: "{{ user_name }}"
    state: "{{ action }}"
    password: "{{ pass }}"
    groups: "{{ group_names }}"
 # tags: 
  #- user
    
 
#- name: Service Details
 # win_service:
  #  name: "{{ name }}"
   # state: "{{ action }}"
  #tags:
  #- service
