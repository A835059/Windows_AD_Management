---
- name : User Details
  win_domain_user:
    name: "{{ name }}"
    state: "{{ state }}"
    password: "{{ pass }}"
    groups: "{{ group_names }}"
 # tags: 
  #- user
    
 
#- name: Service Details
 # win_service:
  #  name: "{{ name }}"
   # state: "{{ action }}"
  #tags:
  #- service
