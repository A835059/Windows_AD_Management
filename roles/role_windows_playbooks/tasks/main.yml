- name: User Details
  win_user:
    name: "{{ item }}"
    password: "{{ password }}"
    groups: "{{ groups }}"
    state: "{{ action }}"
  tags: 
  - user
  
- name: Service Details
  win_service:
    name: "{{ item }}"
    state: "{{ action }}"
  tags:
  - service
