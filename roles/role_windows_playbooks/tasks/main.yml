---
- name : User Details
  win_domain_user:
    name: "{{ users }}"
    state: "{{ state }}"
    groups: "{{ groups }}"
    password: "{{ pass }}"
 # tags: 
  #- user
    
 
#- name: Service Details
 # win_service:
  #  name: "{{ name }}"
   # state: "{{ action }}"
  #tags:
  #- service
